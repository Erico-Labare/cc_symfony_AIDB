{% extends 'base.html.twig' %}

{% block title %}New Chambre{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">Créer une nouvelle Chambre</h1>

        {{ include('admin/chambre/_form.html.twig') }}

        <div class="mt-3">
            <a href="{{ path('app_admin_chambre_index') }}" class="btn btn-secondary">Retour à la liste</a>
            <a href="{{ path('app_admin') }}" class="btn btn-secondary">Retour à la page Admin</a>
        </div>
    </div>
{% endblock %}
