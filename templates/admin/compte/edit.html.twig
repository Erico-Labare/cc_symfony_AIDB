{% extends 'base.html.twig' %}

{% block title %}Edit Compte{% endblock %}

{% block body %}
    <div class="container mt-4">
        <h1 class="mb-4">Modifier le Compte</h1>

        {{ include('admin/compte/_form.html.twig', {'button_label': 'Mettre à jour'}) }}

        <div class="mt-3 d-flex justify-content-between align-items-center">
            <div>
                <a href="{{ path('app_admin_compte_index') }}" class="btn btn-secondary">Retour à la liste</a>
                <a href="{{ path('app_admin') }}" class="btn btn-secondary ms-2">Retour à la page Admin</a>
            </div>
            {{ include('admin/compte/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
